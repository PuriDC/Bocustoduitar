node_modules/
dist/

# Local tooling, not part of the deployment
.claude/
.mcp.json

# Images added through the admin. They exist only on the server, which is also
# why both rsync calls exclude this directory.
backend/uploads/

# Holds JWT_SECRET and database credentials — never commit it.
.env
.env.local
backend/.env
frontend/.env
